# Virtual env
.venv/
venv/
env/

# Database and shards (INSPIRE mirror is multi-GB — use Git LFS or external hosting if you need it in version control)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Data (TEI, PDFs, logs)
data/tei/
data/arxiv_pdfs/
*.pdf

# Work items and worker logs
work_items.txt
work_items_part_*
worker_*.log
*.lock

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# OS
.DS_Store
Thumbs.db

# Corpus embedding outputs: large binaries under dedicated trees only (not global *.parquet)
outputs_no_sync/
outputs/

# Large CSVs (optional: uncomment if you don't want to track them)
# top200_manifest*.csv
# top200_citers_manifest.csv
